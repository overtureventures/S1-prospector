[build]
builder = "NIXPACKS"

[deploy]
startCommand = "echo 'Bots run on cron schedule only'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[deploy.cronJobs]]
schedule = "0 9 * * 3"
command = "python main.py"
